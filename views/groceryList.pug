extends default

block content

  pre= h.dump(items)
  hr
  - let sorted = items.sort((a, b) => a.area - b.area)
  ol
    each item in sorted
      li= `${item.name} (x${item.qty}) in area ${item.area}`