extends default

block content
  h1 It worked 🎉
  h2 Here's what the email sent to 
    if h.getEmailRecipients(formData.emailTo).length === 1
      span= `${h.getEmailRecipients(formData.emailTo)} `
    else if h.getEmailRecipients(formData.emailTo).length === 2
      span= `${h.getEmailRecipients(formData.emailTo)[0]} & ${h.getEmailRecipients(formData.emailTo)[1]} `
    else
      span 🐭 
    span looks like:
  hr
  div!= emailOutput