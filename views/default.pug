doctype html
html
  include includes/head
  body.p1.sm-p2.md-p3
    header.container.sm-flex.flex-center.flex-wrap
      h1.mb0.sm-mb2.mt0.blue
        a(href="/").fw400= h.site.name
      h2.mt0.sm-ml1.h3.fw400.blue= h.site.byline.toLowerCase()
    block messages
      .container
        if locals.flashes
          //- pre= h.dump(locals.flashes)
          - const categories = Object.keys(locals.flashes)
          each category in categories
            each message in flashes[category]
              section(class=`flex flex-center flash flash--${category} mb3`)
                h1.m0.h4.regular.flex-grow!= message
                a.btn.h3.p0.cursor(onClick="this.parentElement.remove()") &times;
    .container
      block content

    .container
      include includes/footer